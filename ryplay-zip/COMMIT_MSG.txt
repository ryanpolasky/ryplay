perf, security, cleanup: remove Spotify lite, /api/clock from=, DNS TOCTOU, centralize artwork

- Remove unused Spotify-OAuth dashboard (frontend + /api/spotify/* backend)
  Keeps the artwork-search Spotify client-credentials fallback as documented
- /api/clock: use Last.fm 'from=' filter instead of paging history; cap pages
  at 5; drop the 0.25s inter-page sleep. ~3.4s -> ~500ms cold path
- /api/artwork: re-validate the connected peer IP after socket open to close
  the DNS TOCTOU window between SSRF check and httpx's own resolution
- Centralize artwork loading via src/lib/artworkLoader: single in-flight
  Promise per URL shared across useArtworkPalette + VinylView; pass the
  loaded HTMLImageElement to node-vibrant so it doesn't refetch
- slowapi per-IP rate limits per endpoint (300/min /api/artwork, 20-60 elsewhere)
- Fail-fast at startup if LASTFM_API_KEY isn't set
- docker-compose.yml + fix nginx.conf and backend/Dockerfile port mismatch
  (8788 -> 8000) so the repo is reproducible
- README: document required and optional env vars, add docker-compose section

